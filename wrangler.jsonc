{
	"name": "backlog-remote-mcp-server",
	"main": "src/platforms/cloudflare/index.ts",
	"compatibility_date": "2025-04-01",
	"compatibility_flags": ["nodejs_compat"],
	"migrations": [
		{
			"new_sqlite_classes": ["BacklogMCP"],
			"tag": "v1"
		}
	],
	"durable_objects": {
		"bindings": [
			{
				"class_name": "BacklogMCP",
				"name": "MCP_OBJECT"
			}
		]
	},
	"kv_namespaces": [
		{
			"binding": "OAUTH_KV",
			"id": "<YOUR_KV_NAMESPACE_ID>"
		}
	],
	// カスタムドメインはここに書かない。デプロイ時に MCP_HOSTNAME から
	// wrangler.deploy.json へ注入される (scripts/build-deploy-config.mjs)。
	// 素の `wrangler deploy` ではカスタムドメインが付かないので必ず
	// `npm run cloudflare:deploy` を使うこと。
	"observability": {
		"enabled": true
	},
	"dev": {
		"port": 8788,
		// ローカル開発時のホスト。routes の custom_domain に上書きされないようにする
		"host": "localhost:8788"
	},
	"secrets": {
		"required": [
			"ACCESS_CLIENT_ID",
			"ACCESS_CLIENT_SECRET",
			"ACCESS_TOKEN_URL",
			"ACCESS_AUTHORIZATION_URL",
			"ACCESS_JWKS_URL",
			"COOKIE_ENCRYPTION_KEY",
			"BACKLOG_SPACES_CONFIG",
			"ALLOWED_EMAILS"
		]
	}
}
