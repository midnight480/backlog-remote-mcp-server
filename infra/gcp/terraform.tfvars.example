# infra/gcp/terraform.tfvars として複製して埋める。
# このファイルは機密値を含むので .gitignore 済み。

project_id      = "your-gcp-project"
region          = "asia-northeast1"
image           = "asia-northeast1-docker.pkg.dev/your-gcp-project/backlog-mcp/backlog-mcp:latest"
public_base_url = "https://backlog-mcp.example.com"
custom_domain   = "backlog-mcp.example.com"

# ツールの利用を許可するメールアドレス
allowed_emails = "[\"you@example.com\"]"

# 上流 IdP。既定は Google アカウント。
# Entra ID を使う場合は upstream_idp = "entra" と upstream_tenant_id を設定する。
upstream_idp           = "google"
upstream_client_id     = "xxxxxxxx.apps.googleusercontent.com"
upstream_client_secret = "GOCSPX-xxxxxxxxxxxxxxxx"
# upstream_tenant_id   = "11111111-2222-3333-4444-555555555555"

# Backlog のスペース設定
backlog_spaces_config = "{\"spaces\":[{\"name\":\"example\",\"domain\":\"example.backlog.com\",\"apiKey\":\"xxx\"}],\"defaultSpace\":\"example\"}"
