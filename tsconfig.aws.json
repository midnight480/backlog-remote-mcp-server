{
	// AWS Lambda 向け。Workers の型を読み込まないので、Workers 固有のグローバルを
	// うっかり使うと型エラーになる。
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"types": ["node"]
	},
	"include": ["src/core/**/*.ts", "src/oauth/**/*.ts", "src/platforms/aws/**/*.ts"]
}
