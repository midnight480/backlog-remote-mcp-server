# サプライチェーン対策: 公開直後のパッケージを掴まない
#
# 悪意あるバージョンが npm に混入する事案は、週末に公開されて数日でテイクダウン
# される形が多い。公開から一定日数が経ったバージョンだけを解決対象にすることで、
# その窓を避ける。
#
# npm 11.6 以降の機能。単位は日。依存を追加・更新するときの「解決」に効く。
# npm ci はロックファイルどおりに入れるだけなので、この設定では守られない
# (ロックに入る前に止めるための設定)。
min-release-age=3
